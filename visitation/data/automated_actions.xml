<?xml version="1.0" encoding="utf-8" ?>
<odoo>
  <data>

    <record id="action_auto_set_scheduled_visit" model="base.automation">
        <field name="name">Visitation: Create a scheduled visit based on requested visit slot</field>
        <field name="active" eval="True"/>
        <field name="model_id" ref="visitation.model_x_visit_request"/>
        <field name="state">code</field>
        <field name="code">
def prepare_new_scheduled_visit(visitors):
  partner_vals = [(6, 0, [])]
  for visitor in visitors:
    partner_vals.append((0, 0, {
      'name': visitor.x_name,
      'email': visitor.x_email,
      'phone': visitor.x_phone,
      'street': visitor.x_street,
      'street2': visitor.x_street2,
      'city': visitor.x_city,
      'zip': visitor.x_zip,
      'state_id': visitor.x_state_id.id,
      'country_id': visitor.x_country_id.id,
    }))
  scheduled = env['x_scheduled_visit'].sudo().create({
    'x_visit_request_id': rec.id,
    'x_visit_availability_slot_id': rec.x_requested_availability_id.id,
    'x_visitor_screening_ids': rec.x_screening_ids.ids,
    'x_resident_bed_id': rec.x_resident_bed_id.id,
    'x_partner_ids': partner_vals,
  })
  return scheduled

def send_confirmation_email(scheduled_visit):
  tmpl = env.ref('visitation.mail_template_confirm_visit')
  if tmpl:
      scheduled.with_context(force_send=True).message_post_with_template(tmpl.id)
  env.ref('mail.ir_cron_mail_scheduler_action').sudo().method_direct_trigger()

for rec in records.sudo():
  if rec.x_requested_availability_id:
    env['x_scheduled_visit'].search([('x_visit_request_id', '=', rec.id)]).unlink()
    scheduled = prepare_new_scheduled_visit(rec.x_screening_ids)
    rec['x_scheduled_visit_id'] = scheduled.id
    scheduled.message_unsubscribe(env.ref('visitation.public_rpc_user').ids)
    scheduled.message_subscribe(scheduled.x_partner_ids.ids)
    send_confirmation_email(scheduled)
        </field>
        <field name="trigger">on_create_or_write</field>
        <field name="trigger_field_ids"
          eval="[ref('field_x_visit_request__x_resident_bed_id'), ref('field_x_visit_request__x_screening_ids'),ref('field_x_visit_request__x_requested_availability_id')]"
          />
        <field name="filter_domain"></field>
    </record>
    
  </data>
</odoo>
